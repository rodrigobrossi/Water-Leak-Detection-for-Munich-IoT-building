CREATE TABLE DBUSER.BDP_TENANT (TENANT_ID INTEGER not null GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1), TENANT VARCHAR(100) not null UNIQUE ENFORCED, TENANT_NAME VARCHAR(100) not null UNIQUE ENFORCED, PRIMARY KEY (TENANT_ID)) ORGANIZE BY ROW;
